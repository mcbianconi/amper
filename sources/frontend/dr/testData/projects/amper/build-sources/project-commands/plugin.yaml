tasks:
  updateGoldFiles:
    action: !updateGoldFiles
      amperRootDir: ${project.rootDir}
      versions: ${pluginSettings.versions}

  syncVersions:
    action: !syncVersions
      amperRootDir: ${project.rootDir}
      versions: ${pluginSettings.versions}

commands:
  - updateGoldFiles
  - syncVersions
