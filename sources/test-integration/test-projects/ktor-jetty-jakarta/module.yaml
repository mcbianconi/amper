product: jvm/app

settings:
  ktor: enabled
  jvm:
    mainClass: org.jetbrains.amper.test.ktor.ApplicationKt

dependencies:
  - $ktor.server.core
  - $ktor.server.jetty
  - ch.qos.logback:logback-classic:1.5.18

test-dependencies:
  - $ktor.server.testHost
